# CodeDeploy resources are intentionally disabled because the current AWS identity
# does not have permissions to create IAM roles or CodeDeploy applications.
# This keeps the infrastructure deployment working under the existing user privileges.
